// The namespace 'Lesson2' is the root namespace defined in the *.sln file
namespace Lesson2
{
    // The class 'Program' with an access modifier of 'public' (template default: internal) so we can call the Main method in the test
    public class Program
    {
        // The static modifier indicating this method is not dependent on any instance of its parent class
        // The args parameter with the reference type that is an array of strings
        public static void Main(string[] args)
        {
            // TODO: Write 'Hello World' to the console
            // When you run the test it should pass if done correctly
        }
    }
}